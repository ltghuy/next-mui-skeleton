'use client';

import { CoreButton } from '@/components/core';

const HomePage = () => {
  return (
    <main>
      This is Homepage
      <CoreButton>Click here</CoreButton>
    </main>
  );
};

export default HomePage;
